class Solution {
    public int[] getConcatenation(int[] nums) {
        int[] res = Arrays.copyOf(nums,nums.length+nums.length);
        System.arraycopy(nums,0,res,nums.length,nums.length);
        return res;
    }
}
